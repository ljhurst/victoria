# victoria-viewer

Read-only web viewer for the wiki — a file browser and markdown renderer so
a human can follow along with what the agent writes. Runs as its own Lambda
behind Lasso OAuth. Built into `agent/dist/victoria-viewer.zip` by
`scripts/build_lambda.sh viewer`.
