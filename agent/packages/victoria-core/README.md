# victoria-core

Shared core for Victoria: S3 wiki storage, the FTS5 search index, the
`remember`/`consolidate` agent operations, and Lasso token verification.
Consumed by `victoria-mcp` and `victoria-viewer`.
